---
galaxy_info:
  role_name: ai-gateway
  description: "AI Gateway — unified agentic backend for IIAB (FastAPI + LangGraph)"
  author: deldesir
  license: AGPL-3.0
  min_ansible_version: "2.10"

dependencies:
  - role: nginx
  - role: litellm
    when: litellm_install | bool
