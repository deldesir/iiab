---
galaxy_info:
  role_name: rivebot
  description: "RiveBot — RiveScript brain service for deterministic pre-LLM intent routing in IIAB"
  author: deldesir
  license: AGPL-3.0
  min_ansible_version: "2.10"

dependencies:
  - role: ai-gateway
    when: ai_gateway_install | bool
