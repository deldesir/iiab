---
galaxy_info:
  role_name: hermes-agent
  description: "Hermes Agent — autonomous reasoning engine with tool calling (in-process library for AI Gateway)"
  author: deldesir
  license: AGPL-3.0
  min_ansible_version: "2.10"

dependencies:
  - role: ai-gateway
    when: ai_gateway_install | bool
