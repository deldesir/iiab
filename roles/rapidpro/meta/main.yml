dependencies:
  - role: nginx
    vars:
      nginx_install: true
  - role: postgresql
    vars:
      postgresql_install: true
      postgresql_enabled: true
  - role: wuzapi
    when: wuzapi_install | default(true)
    vars:
      wuzapi_enabled: true
